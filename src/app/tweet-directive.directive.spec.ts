import { TweetDirectiveDirective } from './tweet-directive.directive';

describe('TweetDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TweetDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
